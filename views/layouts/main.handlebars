<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    {{#if title}}
    <title>{{title}}</title>
    {{else}}
    <title>Default Title.</title>
    {{/if}}
</head>

<body>
    {{{body}}}
</body>

</html>