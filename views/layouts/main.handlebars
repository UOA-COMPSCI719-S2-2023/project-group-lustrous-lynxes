<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    {{#if title}}
    <title>{{title}}</title>
    {{else}}
    <title>Lustrous Lynxes</title>
    {{/if}}

    <link rel="stylesheet" href="./css/index.css">

    {{#if includeTinyMCEScripts}}
    {{> tinyMCE-partial}}
    {{/if}}
</head>

<body>
    <header>
        <nav class="navbar">
            <a href="#">Home</a>
            <a href="#">Article</a>
        </nav>

{{#if toastMessage}}
    <p>{{toastMessage}}</p>
{{/if}}
    {{{body}}}
</body>

</html>